version: '3.8'

services:
  f1poolers:
    build: .
    container_name: f1poolers
    restart: unless-stopped
    ports:
      - "3080:3000"  # Frontend + API via Nginx
    volumes:
      - f1poolers_data:/app/server  # Persist database
    environment:
      - NODE_ENV=production

volumes:
  f1poolers_data:
